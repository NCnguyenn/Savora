const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const root=path.join(__dirname,'..'),read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('legacy platform bridge is removed after focused APIs own each role',()=>{for(const file of ['components/customer_footer.php','components/restaurant_footer.php','components/driver_footer.php'])assert.doesNotMatch(read(file),/platform_bridge/);assert.ok(!fs.existsSync(path.join(root,'api/platform_state.php')));assert.ok(!fs.existsSync(path.join(root,'js/platform_bridge.js')));});
test('focused APIs reject removed cross-role commands at their own boundaries',()=>{const dispatch=read('api/dispatch.php'),security=read('lib/request_security.php');assert.match(dispatch,/savora_require_idempotency_key/);assert.match(security,/admin_verify_csrf/);for(const x of ['place_order','restaurant_order_status','driver_milestone'])assert.doesNotMatch(dispatch,new RegExp(x));});
test('Customer checkout and role operation pages use authoritative command boundaries',()=>{assert.match(read('customer_checkout.php'),/api\/checkout\.php/);assert.match(read('js/restaurant_orders.js'),/api\/orders\.php/);assert.match(read('js/driver_delivery.js'),/api\/orders\.php/);});
